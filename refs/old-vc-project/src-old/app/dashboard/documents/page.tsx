import { DocumentsPage } from "@/components/dashboard/DocumentsPage";

/**
 * Documents page - now uses consolidated DocumentsPage component
 * Follows Scandinavian minimalism: semantic text-driven, ultra-sparse colors, monospaced financial data
 */
export default function DocumentsPageRoute() {
  return <DocumentsPage />;
}
